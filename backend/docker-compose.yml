services:
  postgres:
    image: postgres:16
    container_name: flowpass-postgres
    environment:
      POSTGRES_DB: flowpass
      POSTGRES_USER: flowpass
      POSTGRES_PASSWORD: flowpass
    ports:
      - "5432:5432"
    volumes:
      - flowpass_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U flowpass -d flowpass"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  flowpass_pgdata:
